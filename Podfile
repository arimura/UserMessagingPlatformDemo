# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'UserMessagePlatformDemo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for UserMessagePlatformDemo
  pod 'Google-Mobile-Ads-SDK'

  target 'UserMessagePlatformDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'UserMessagePlatformDemoUITests' do
    # Pods for testing
  end

end
